{
  "name": "whats-for-dinner",
  "version": "1.0.0",
  "description": "Premium meal planning web app with OCR and cuisine selection",
  "main": "index.html",
  "scripts": {
    "dev": "npx serve -l 5000",
    "test": "jest",
    "build": "echo \"No build step – static files only\"",
    "deploy": "npm run build && npx gh-pages -d ."
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.0.0",
    "serve": "^14.0.0",
    "gh-pages": "^5.0.0"
  }
}
