{
  "name": "whats-for-dinner",
  "version": "1.0.0",
  "description": "Premium meal planning web app with OCR and cuisine selection",
  "main": "index.html",
  "scripts": {
    "dev": "npx serve -l 5000",
    "test": "jest",
    "build": "rm -rf dist && mkdir dist && cp index.html dist/ && cp -r css dist/ && cp -r js dist/ && cp -r .github dist/.github || true",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.0.0",
    "jsdom": "^22.0.0",
    "gh-pages": "^5.0.0"
  }
}
